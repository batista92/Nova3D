# Nova3D.Physics.Bepu

Optional BEPUphysics 2 integration for Nova3D. It provides a fixed-timestep
physics world, rigid bodies, primitive colliders, queries and explicit
conversions between MonoGame and `System.Numerics` math types.

The core `Nova3D` package does not depend on this module.
